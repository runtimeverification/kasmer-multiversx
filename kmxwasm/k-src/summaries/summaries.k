// This file exists only so that it is replaced when analyzing a contract
// with another one that contains summarizations for all functions.
//
// It lives in its own directory because the current way of replacing the
// file is to change the include paths for kompile.
module SUMMARIES
endmodule
